# masterhack
Master Card Hackathon Repo

Preparations:

- Have Node.js installed

```
> npm install -g bower
> npm install -g grunt-cli
> npm install -g cordova
```

Client:

```
> cd client
> npm install
> bower install
```